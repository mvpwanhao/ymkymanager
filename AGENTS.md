# 协作与自动化助手约定（本仓库）

- **本地运行**：在用户希望「本地试」或未声明仅文档修改时，应使用 **`python -m uvicorn app.main:app --host 127.0.0.1 --port 8080 --reload`**（或 **`scripts/dev.ps1`**）。`--reload` 会在保存源码后自动重载进程；若已有该进程在跑，通常无需再重启。
- **端口占用 / 权限错误**：可改用 `--port 8081`（或本机未被占用的端口）。
- **`git push` / 远端部署**：仅当用户**明确说明**时再执行。
- **Cursor**：个人可把本地规则写在 `.cursor/rules/`（仓库默认 `.gitignore` 忽略）；团队共享的机器可读本文件以对齐约定。
